# A) Tracciabilità US-01..05 × codice × engine

Fonte US: `PROJECTS/Slay the Roadmap/EGS_assignment2_From User Stories to Sprint 1 Prototype-1.pdf` §1 + §3. Sprint1: US-01 P5/M, US-02 P5/L, US-03 P4/M. US-04/05 fuori Sprint1 ma richieste per esame finale.

| US | Acceptance (esatti) | Stato codice (evidenza) | Engine mapping (mancante) |
|---|---|---|---|
| US-01 roadmap albero Dart | gerarchia indentata; stati bloccato/in corso/completato; opzionali/obbligatori; click-to-expand | **Implementato (dati locali)**: `roadmap_repository:15-130` albero 3+9 topic con prereq; `roadmap_view_model:60-76` unlock ricorsivo; `ui/widgets/roadmap/roadmap_tree:9-27` + `topic_tile:7-76` indent `level*20`. Limite: `update/unlock:139-154` solo `print()`, stato solo memoria | `unlock_area`, `unlock_boss`. Nessun `execute`. Da definire `data:{node_id}` |
| US-02 quiz + sblocco | 3-5 domande/topic; soglia 80%; feedback immediato; badge; sblocco automatico | **Implementato parziale dati**: `quiz:13` + `quiz_repo:13` threshold 80; `quiz_repo:180-181` percentage/passed; `quiz_screen:195-260` result; `roadmap_screen:65` sblocco solo se passed. **Gap dati**: solo 3 quiz, altri `throw:159`. `quiz_questions.dart` alternativo rotto (`selectedAnswer`, `submitQuizResults` mancanti) | `quiz_completed → earn_xp data:{xpAmount,score,passed}`. Point `xp` daily+weekly, levels `experience` (L1 0, L2 100, L3 500), badge collection. Rule `quiz_pass_threshold_80.drl` |
| US-03 reward 1 su 3 | 3 tipi diverse; preview effetti; inventory visibile; limite 1/topic | **Parziale (logica senza UI)**: `reward_repo:75-77 take(3)`, boss con 3 `availableRewards`, `reward.isSelected` mai settato. `quiz_screen` non offre reward; `boss_fight_active:422-423 TODO Show reward selection + pop` | `claim_reward → unlock_card/unlock_skill`. Decidere: carte come badge engine o locali + solo `earn_xp`. Rule `reward_claim_once.drl` |
| US-04 boss quiz adattivi | HP bar; turno player carte; turno boss quiz-danno; soglie 25/50/75%; win/lose con conseguenze | **Implementato (locale, da bilanciare)**: `boss_fight:39-54` % + soglie; `boss_vm:119` danno quiz `percentage/10`, `:183-217` danni 10/20/heal15/poison5, `:150` delay 1500ms, azione random; `boss_health_bar`, `player_health_bar`, `action_card` presenti. Win/lose senza reward, senza schermate dedicate | `play_card {card_id,damage,cost}`, `boss_quiz_answer {correct}`, `boss_defeated/boss_lost → earn_xp + badge mid_boss_badge`. Rules `boss_damage`, `boss_defeat`, `boss_threshold` |
| US-05 autosave | save topic/deck/boss; restore all'avvio; reset opzionale | **Mancante (codice morto)**: `player_progress toJson/fromJson` OK ma `fromJson` perde `adaptiveQuizzes:195`; `SharedPreferencesService` OK ma mai chiamato, `setupLocator` mai invocato, dipendenze assenti in pubspec → 4 error. Nessun save dopo quiz/boss/topic | Save locale. Opzionale `sync_progress`. Rischio disallineamento locale vs `xp` daily/weekly se engine aggiunto dopo |

Nota Game Actions: `Element, Actions, Mockups/Game Actions, Game Elements & Mockups.pdf` analizzato **non è Slay** (healthy-habit/avatar/store) → mapping trigger/behavior/feedback per Slay va ricostruito dalle 8 azioni implicite (read post, unlock boss, play cards, boss quiz, win, lose, unlock area, take dmg). Mockup Assignment2 pp.4-7 vuote in estrazione testo (probabili immagini) → verificare visivamente prima del video.
