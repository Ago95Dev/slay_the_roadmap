# Utente U2 — scheda risultati (DA COMPILARE A SESSIONE SVOLTA)

- Data: —
- Profilo: età —, esperienza Dart/Flutter —, mai vista l'app: sì/no —
- Task (R = riuscito, F = fallito + tempo): T1 —, T2 —, T3 —, T4 —, T5 —, T6 —, T7 —
- SUS (0-100): —
- IMI (interesse / competenza / pressione, 1-5): — / — / —
- GEQ (flow / sfida / positivo / negativo, 1-5): — / — / —
- 3 attriti osservati:
  1. —
  2. —
  3. —
- 3 citazioni testuali:
  1. "—"
  2. "—"
  3. "—"
- Bug visti: —
